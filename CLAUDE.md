# CLAUDE.md — ulpExtractor

Git repo is this directory. Run all git and cargo commands from here using:
  git -C /home/kali/gitaction/ulpKop/ulpExtractor <args>
  cargo build --release --manifest-path /home/kali/gitaction/ulpKop/ulpExtractor/Cargo.toml

Or cd into this directory first:
  cd /home/kali/gitaction/ulpKop/ulpExtractor && <command>

The parent directory (/home/kali/gitaction) has its own .git repo — ignore it.
This project has its own independent git history and tags.
